* {
    border: none;
    border-radius: 0;
    font-family: "JetBrainsMono Nerd Font", "Font Awesome 6 Free", sans-serif;
    font-size: 13px;
    min-height: 0;
}

/* Ana Bar */
window#waybar {
    background-color: rgba(125, 207, 255, 0); /* Tokyo Night Arka Plan */
    border-bottom: 0px solid #1a1b26;
    color: #c0caf5;
    transition-property: background-color;
    transition-duration: 0s;
}

/* Workspace */
#workspaces {
    margin: 4px;
    background-color: #2a2a2a;
    border-radius: 8px;
}

#workspaces button {
    padding: 0 8px;
    color: #cfc9c2;
}

#workspaces button.focused {
    color: #3b4252;
    background-color: #1a1a1a;
    border-bottom: 2px solid #2b2b2b;
}

#workspaces button.urgent {
    color: #f7768e;
}

/* Tüm Modüller İçin Ortak Stil */
#clock, #battery, #pulseaudio, #network, #cpu, #memory, #backlight, #language, #tray, #custom-mem {
    padding: 0 12px;
    margin: 4px 2px;
    background-color: #2a2a2a;
    border-radius: 8px;
}

/* Özel Renk Vurguları */
#clock { color: #cfc9c2; }
#pulseaudio { color: #cfc9c2; }
#battery { color: #cfc9c2; }
#battery.charging { color: #9ece6a; }
#battery.critical:not(.charging) { color: #f7768e; }
#backlight { color: #cfc9c2; }
#tray { background-color: #2a2a2a; }
#language { color: #cfc9c2; }
#custom-mem { color: #cfc9c2; }

